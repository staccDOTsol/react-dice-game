import { Component } from 'react';
declare class CustomStrategyDetailContainer extends Component {
    handleChange: ({ currentTarget: { checked, id, value } }: {
        currentTarget: {
            checked: any;
            id: any;
            value: any;
        };
    }) => void;
    render(): JSX.Element;
}
export default CustomStrategyDetailContainer;
